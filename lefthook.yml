pre-commit:
  parallel: true
  commands:
    prettier:
      glob: "*.{js,ts,jsx,tsx,mjs,mts,json,yaml,md,prettierrc}"
      run: pnpm dlx prettier --write {staged_files}
      stage_fixed: true
pre-push:
  parallel: true
  commands:
    audit-check:
      run: pnpm audit
